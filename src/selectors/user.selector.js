export const isUsersLoaded = state => state.isLoaded;

export const getUsers = state => state.users;

export const isUseresHasError = state => state.error;

export const getUsersFiltersName = state => state.filters.name;
